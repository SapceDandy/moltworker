name: export_csv
description: Download the leads database as a CSV file.
type: http

request:
  method: GET
  url: "${BASE_URL}/api/export.csv"
  headers:
    Authorization: "Bearer ${MOLTBOT_GATEWAY_TOKEN}"

response:
  type: text
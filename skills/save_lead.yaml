name: save_lead
description: Save a lead row to the database.
type: http

request:
  method: POST
  url: "{{BASE_URL}}/api/leads"
  headers:
    Content-Type: application/json
    Authorization: "Bearer ${MOLTBOT_GATEWAY_TOKEN}"
  body: "{{lead_json}}"

response:
  type: json